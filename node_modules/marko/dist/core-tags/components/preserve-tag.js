var ComponentsContext = require("../../runtime/components/ComponentsContext");
var getComponentsContext = ComponentsContext.r_;

module.exports = function render(input, out) {
  var shouldPreserve = Boolean(!("if" in input) || input["if"]);
  var ownerComponentDef = out.l_;
  var ownerComponent = ownerComponentDef.m_;
  var key = out.n_;

  out.p_(key, ownerComponent, true);

  if (input.renderBody) {
    var componentsContext = getComponentsContext(out);
    var parentPreserved = componentsContext.s_;
    componentsContext.s_ = parentPreserved || shouldPreserve;
    input.renderBody(out);
    componentsContext.s_ = parentPreserved;
  }

  out.q_();
};