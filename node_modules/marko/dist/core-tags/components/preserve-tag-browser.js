module.exports = function render(input, out) {
  var componentsContext = out.h_;
  var isHydrate = componentsContext && componentsContext.j_.k_;
  var ownerComponentDef = out.l_;
  var ownerComponent = ownerComponentDef.m_;
  var key = out.n_;
  var shouldPreserve = !("if" in input) || input["if"];
  var isPreserved = Boolean(shouldPreserve && (isHydrate || ownerComponent.o_[key]));

  out.p_(key, ownerComponent, shouldPreserve);

  if (!isPreserved && input.renderBody) {
    input.renderBody(out);
  }

  out.q_();
};