function KeySequence() {
  this._U_ = Object.create(null);
}

KeySequence.prototype._I_ = function (key) {
  var lookup = this._U_;

  if (lookup[key]) {
    return key + "_" + lookup[key]++;
  }

  lookup[key] = 1;
  return key;
};

module.exports = KeySequence;